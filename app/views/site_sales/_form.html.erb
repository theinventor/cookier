<%= form_for(@site_sale) do |f| %>
  <% if @site_sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_sale.errors.count, "error") %> prohibited this site_sale from being saved:</h2>

      <ul>
      <% @site_sale.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :start_at %><br />
    <%= f.datetime_select :start_at %>
  </div>
  <div class="field">
    <%= f.label :end_at %><br />
    <%= f.datetime_select :end_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
